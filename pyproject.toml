[project]
name = "rpg"
version = "0.1.0"
description = "Collection of RPG tools and toys"
readme = "README.rst"
requires-python = ">=3.14.6"
# personal packages
# rpglib - in this directory
# dice - in this directory, not a package, symlinked where needed.
# die
dependencies = [
  "click",
  ]
